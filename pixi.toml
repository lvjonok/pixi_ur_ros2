[workspace]
authors = ["Lev Kozlov <kozlov.l.a10@gmail.com>"]
name = "pixi_ur_ros2"
version = "0.1.0"
channels = ["conda-forge", "robostack-jazzy"]
platforms = ["linux-64"]

[activation]
scripts = ["scripts/set_ros_env.sh", "install/setup.bash"]
[dependencies]
python = "*"
compilers = "*"
cmake = "*"
pkg-config = "*"
make = "*"
ninja = "*"

[target.linux.dependencies]
libgl-devel = "*"

[environments]
jazzy = { features = ["jazzy", "dev"] }
inference = { features = ["jazzy", "inference", "dev"] }

# ROS2 Jazzy Feature
[feature.jazzy]
channels = ["robostack-jazzy"]

[feature.jazzy.dependencies]
colcon-common-extensions = "*"
pinocchio = "*"
ros-jazzy-desktop = "*"
ros-jazzy-ur = ">=3.7.0, <4" # crucial for torque control
ros-jazzy-joint-state-publisher = "*"
ros-jazzy-rmw-cyclonedds-cpp = "*"
ros-jazzy-ros2-control = "*"
ros-jazzy-ros2-controllers = "*"
ros-jazzy-xacro = "*"
ros-jazzy-topic-tools = "*"
ros-jazzy-topic-tools-interfaces = "*"

[feature.jazzy.tasks]
clone-crisp = "test -d src/crisp_controllers || git clone https://github.com/utiasDSL/crisp_controllers.git src/crisp_controllers"
build = "colcon build --cmake-args -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF -DBUILD_TESTS=OFF -DBUILD_EXAMPLES=OFF -DCMAKE_POLICY_VERSION_MINIMUM=3.5 -Wno-dev"
setup = { depends-on = ["clone-crisp", "build"] }
launch = "ros2 launch crisp_ur_bringup ur.launch.py"

[feature.jazzy.pypi-dependencies]
filelock = ">=3.25.2, <4"

# Inference Feature
[feature.inference]
channels = ["robostack-jazzy"]

[feature.inference.dependencies]
numpy = "<=2.3" # from crisp_py

[feature.inference.pypi-dependencies]
crisp-python = ">=3.4.0"

# Development tools
[feature.dev.pypi-dependencies]
ruff = "*"
pytest = "*"
